import styles from "./Header.parts.module.scss";

const HeaderComponent: React.FC = () => {
  return <div className={styles.headerContainer}>HeaderComponent</div>;
};

export default HeaderComponent;
